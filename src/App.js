import React from 'react';
import Home from './components/Home';

const App = () => {
  return (
    <div className="bg-gray-900 min-h-screen">
      {/* Home Section (already includes Skills, Projects, Contact) */}
      <Home />
    </div>
  );
};

export default App;
